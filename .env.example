# Copy to .env — NEVER commit .env

BINANCE_API_KEY=
BINANCE_API_SECRET=

# mainnet = real money | demo = Binance demo API | testnet = vision testnet
BINANCE_ENV=mainnet

# Must be 1 to allow orders when --live is passed
BINANCE_LIVE=0
