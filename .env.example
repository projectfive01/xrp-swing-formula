# Copy to .env and fill in. NEVER commit .env
# Start with TESTNET keys from https://testnet.binance.vision/

BINANCE_API_KEY=
BINANCE_API_SECRET=

# testnet | mainnet
BINANCE_ENV=testnet

# Must be exactly 1 to allow real orders when --live is passed
BINANCE_LIVE=0
