# Copy to .env — NEVER commit .env

BINANCE_API_KEY=
BINANCE_API_SECRET=

# us = Binance.US (your account) | mainnet = international | demo | testnet
BINANCE_ENV=us

# Must be 1 to allow real orders when --live is passed
BINANCE_LIVE=0
